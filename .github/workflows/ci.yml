name: CI

on:
  push:
  pull_request:
    branches: [main, master]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase local stack
        run: supabase start

      - name: Export Supabase env for Python
        run: |
          eval "$(supabase status -o env)"
          echo "SUPABASE_URL=${API_URL}" >> "$GITHUB_ENV"
          echo "SUPABASE_ANON_KEY=${ANON_KEY}" >> "$GITHUB_ENV"
          echo "SUPABASE_SERVICE_ROLE_KEY=${SERVICE_ROLE_KEY}" >> "$GITHUB_ENV"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Pylint
        run: pylint app --errors-only

      - name: Pytest with coverage
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
        run: coverage run -m pytest tests
      
      - name: Start Backend Server
        run: |
          export PYTHONPATH=$GITHUB_WORKSPACE/backend
          python -c "from app.app import app; app.run(host='127.0.0.1', port=5002)" &
          timeout 60 bash -c 'until curl -s http://127.0.0.1:5002/ > /dev/null; do sleep 2; done'

      - name: Integration tests
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
          E2E_BASE_URL: http://127.0.0.1:5002
        run: pytest local_tests

      - name: Coverage report
        run: coverage report -m
      
      - name: Coverage XML
        run: coverage xml
      
      - name: Coverage HTML
        run: coverage html
